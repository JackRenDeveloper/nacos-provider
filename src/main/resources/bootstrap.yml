spring:
  application:
    name: nacos-provider
  cloud:
    nacos:
      config:
        server-addr: 192.168.4.201:18083
        file-extension: yaml
        prefix: nacos-provider
  profiles:
    active: dev
spring:
  application:
    name: nacos-provider
  cloud:
    nacos:
      config:
        server-addr: elvis.ziyun56.com:18483
        file-extension: yaml
        prefix: nacos-provider
  profiles:
    active: dev